<?xml version="1.0" encoding="UTF-8"?>
<flow>
    <!-- 通用风控决策流程（信号驱动架构） -->
    <chain name="riskDecisionFlow">
        THEN(
            loadContext,
            WHEN(newDeviceCheck, oddHourCheck, failCountCheck),
            decisionJudge
        );
    </chain>
</flow>
